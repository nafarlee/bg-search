kind: Ingress
apiVersion: networking.k8s.io/v1
metadata:
  name: bg-search
spec:
  rules:
    - host: bgs.nafarlee.dev
      http:
        paths:
          - path: /*
            pathType: ImplementationSpecific
            backend:
              service:
                name: bg-search
                port:
                  number: 80
