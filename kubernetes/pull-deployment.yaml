kind: Deployment
apiVersion: apps/v1
metadata:
  name: pull
spec:
  selector:
    matchLabels:
      app: pull
  template:
    metadata:
      labels:
        app: pull
        type: cron
    spec:
      terminationGracePeriodSeconds: 15
      containers:
        - name: default
          image: ghcr.io/nafarlee/bg-search-pull-games:d1fe91ec11ffe69ed3aafd65a07d7571c09d934e
          env:
            - name: PGUSER
              value: postgres
            - name: PGHOST
              value: database
            - name: BGG_API_KEY
              valueFrom:
                secretKeyRef:
                  name: bgg-api-key
                  key: bgg-api-key
            - name: PGPASSWORD
              valueFrom:
                secretKeyRef:
                  name: postgres-password
                  key: postgres-password
