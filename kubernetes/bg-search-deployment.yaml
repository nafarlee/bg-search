kind: Deployment
apiVersion: apps/v1
metadata:
  name: bg-search
spec:
  selector:
    matchLabels:
      app: bg-search
  template:
    metadata:
      labels:
        app: bg-search
    spec:
      nodeSelector:
        cloud.google.com/gke-spot: "true"
      terminationGracePeriodSeconds: 15
      containers:
        - name: default
          image: ghcr.io/nafarlee/bg-search:2b52db70db5420fe7a9492a06b8e6cc0531fe349
          ports:
            - containerPort: 8080
          resources:
            requests:
              cpu: 75m
              memory: 94371840
          env:
            - name: MIRROR_BASE_URL
              value: https://mirror.staging.bgs.nafarlee.dev
            - name: PGUSER
              value: postgres
            - name: PGHOST
              value: database
            - name: PGPASSWORD
              valueFrom:
                secretKeyRef:
                  name: postgres-password
                  key: postgres-password
