kind: Deployment
apiVersion: apps/v1
metadata:
  name: pull-plays
spec:
  selector:
    matchLabels:
      app: pull-plays
  template:
    metadata:
      labels:
        app: pull-plays
        type: cron
    spec:
      terminationGracePeriodSeconds: 15
      containers:
        - name: default
          image: ghcr.io/nafarlee/bg-search-pull-plays:7b801db90cbe6edb7c93b94d3d642cf58efd7c7d
          env:
            - name: PGUSER
              value: postgres
            - name: PGHOST
              value: database
            - name: BGG_API_KEY
              valueFrom:
                secretKeyRef:
                  name: bgg-api-key
                  key: bgg-api-key
            - name: PGPASSWORD
              valueFrom:
                secretKeyRef:
                  name: postgres-password
                  key: postgres-password
