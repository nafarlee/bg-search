kind: Ingress
apiVersion: networking.k8s.io/v1
metadata:
  name: mirror
spec:
  rules:
    - host: mirror.bgs.nafarlee.dev
      http:
        paths:
          - path: /*
            pathType: ImplementationSpecific
            backend:
              service:
                name: mirror
                port:
                  number: 80
